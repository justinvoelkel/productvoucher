const joi = require('joi')
const product = require('./product')

const schema = joi.object({
  code: joi
    .string()
    .alphanum()
    .length(10)
    .required(),
  sku: product.sku
}).with(code, sku)

module.exports = schema
